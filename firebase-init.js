// Inicialização via CDN movida para index.html (este arquivo não é mais usado)
// Mantido para compatibilidade, porém sem executar nada.
export function initFirebase() { return window.__vts || {}; }
